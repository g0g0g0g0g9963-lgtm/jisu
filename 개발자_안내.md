# 개발자 안내 — 이 코드가 이제 기준 버전입니다

만드신 회의실 예약 앱(2026-08-12 최종본)에 **서버 저장 + Microsoft SSO**를 붙여
사내 NAS에서 실제 서비스 중입니다: `https://192.168.100.25:3501`

앞으로의 개발은 **이 폴더를 기준으로** 이어가 주세요. 이전 버전(vinext/ChatGPT Sites)
위에서 작업하면 서버 연동이 다시 사라져서 매번 재이식을 해야 합니다.

## 원본에서 달라진 점

| 항목 | 원본 (ChatGPT Sites) | 현재 (NAS 서비스) |
| --- | --- | --- |
| 실행 환경 | vinext (Cloudflare Workers) | Vite + Express (Node 24, Docker) |
| 예약 데이터 | React state (새로고침하면 소실) | 서버 SQLite — 전 직원 공유, 30초 자동 동기화 |
| 사용자 | 이름 직접 입력 | Microsoft 365 로그인, 예약자 자동 지정 |
| 예약 취소 | 화면에서만 삭제 | 서버 삭제 + 본인(로그인 이메일) 확인 |

**화면·스타일·`app/lib` 로직은 그대로입니다.** 바뀐/추가된 파일만 정리하면:

- `app/page.tsx` — 예약 목록을 서버에서 읽고, 등록·취소를 서버로 보내도록 6곳 수정.
  로그인 사용자(`currentUser`)가 있으면 예약자 입력창 대신 계정 이름을 표시.
- `app/lib/api.ts` **(신규)** — 서버 통신 전부가 이 파일에 모여 있음
- `server/` **(신규)** — Express API + SQLite + SSO. 회의실·운영시간·시드 데이터는
  프런트와 **같은 `app/config/*.json`을 읽습니다** (한 곳만 고치면 양쪽 반영)
- `standalone/` — 브라우저 전용 진입점 (원본의 standalone 빌드 방식 그대로)
- 빠진 것: vinext·worker·drizzle·examples 등 Cloudflare 전용 파일, `tests/`
  (로직 파일이 그대로라 원하시면 tests를 다시 붙일 수 있습니다)

## 로컬 개발 방법

```bash
npm install
npm run dev:api        # 터미널 1 — API 서버 (3000)
npm run dev:web        # 터미널 2 — Vite 개발 서버 (5173, /api 프록시)
```

브라우저에서 `http://localhost:5173`. **SSO 환경변수가 없으면 익명 모드**(이름 직접
입력)로 동작하므로 로컬에서는 로그인 없이 개발하면 됩니다. 예약 데이터는 `./data/`에
SQLite 파일로 생깁니다 (지우면 초기화, `SEED_DEMO=1`로 실행하면 예시 데이터 주입).

배포 전 확인: `npm run typecheck` + `npm run build`

## 개발할 때 지켜 주세요

1. **예약 상태를 로컬 state로 되돌리지 마세요.** `bookings`는 서버가 원본이고
   화면은 사본입니다. 데이터 조작은 반드시 `app/lib/api.ts`를 통해서.
2. **`app/config/*.json` 구조 변경 시 서버도 확인.** `server/config.mjs`가 같은 파일을
   읽습니다. 키 이름을 바꾸면 `server/index.mjs`(검증)·`server/seed.mjs`(시드)도 수정.
3. **API 계약** (프런트·서버 동시 수정 필요):

   | 메서드 | 경로 | 설명 |
   | --- | --- | --- |
   | GET | `/api/me` | 로그인 사용자 `{user: {name,email} \| null}` |
   | GET | `/api/bookings` | 전체 예약 (`?from=&to=` 기간 조회) |
   | POST | `/api/bookings` | 등록. `dates[]`로 반복 예약, 겹치면 409+conflict |
   | DELETE | `/api/bookings/:id` | 취소. SSO 시 로그인 이메일로 본인 확인 |

4. `Dockerfile`·`docker-compose.yml`·`server/auth.mjs`는 NAS 환경(사내망 DNS 제약,
   권한, 역방향 프록시)에 맞춰져 있으니 **수정 전에 김진규 책임과 상의** 부탁드립니다.

## 전달 방법

개발 완료본은 **이 폴더 구조 그대로 zip**으로 김진규 책임에게 전달해 주세요
(`node_modules`, `dist`, `data`, `.env` 제외 — `.gitignore` 기준).
받는 쪽에서 원클릭 배포 도구로 NAS에 올립니다.

## 이 패키지에 없는 것 (의도적)

- `.env` — SSO 비밀키가 들어가는 파일. NAS에만 존재합니다. 로컬 개발엔 필요 없음.
- `data/` — 운영 예약 DB. 로컬에선 실행 시 자동 생성됩니다.
